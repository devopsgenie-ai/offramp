# Status board

Generated on Emergent. FastAPI backend, Create React App frontend, MongoDB.

## Run locally

```
cd backend && uvicorn server:app --host 0.0.0.0 --port 8001
cd frontend && yarn start
```
